---
name: foundry-tidas-import
description: Use TianGong Foundry to import an external LCA dataset package or develop TIDAS data from source evidence, continue an existing Foundry task, resolve its semantic work items, and complete authorized writes with verified readback.
---

# TianGong Foundry

Use this as the ordinary Foundry entry for both packaged imports and source-evidence development. Foundry owns conversion, context preparation, task state, validation, authorization, execution and recovery. The skill selects inputs, authors evidence-backed decisions, and follows the current public task result.

## Select the task and runtime

Use the qualified managed Foundry runtime selected by the distributed release lock. Keep the writable user workspace separate from the installed skill/runtime. Do not discover a sibling checkout, build a runtime from source, select a floating package or derive a trust anchor from workspace files.

Launch with the skill's unchanged `scripts/tiangong-runtime-bootstrap.sh` on POSIX or `scripts/tiangong-runtime-bootstrap.ps1` on Windows, passing the public Foundry arguments from the reference. The script reads its adjacent trusted `bootstrap-lock.json` and manages the selected user-cache runtime without global Node, pnpm or Git. If the lock is missing or invalid, obtain the complete qualified skill distribution; do not download or invent a substitute lock or override Windows execution policy.

Read [the public task workflow](references/task-workflow.md) to initialize a workspace, start a task or resume one. Keep the user's original request, actor, intended account and input selection. For an existing task, inspect its status before taking another action; do not start a replacement task to escape a blocker or an uncertain write.

Before a new data task, turn the user's request and supplied material into a short task brief: why the result is needed, which objects and boundaries it covers, the intended deliverables, and material principles or constraints. Keep the user's exact request and evidence sources traceable. Label an inference as an AI assumption, not a user requirement or source fact. Reuse what is already clear; ask only for a gap that changes the work. A status check or resumed task uses its registered brief and decisions rather than starting the interview again. Select a brief through the current runtime's public contract only when that runtime exposes it; the distributed Foundry 0.1.10 lock does not establish support for a newer interaction input.

- **Packaged import:** select the original supported archive or dataset directory. Let Foundry invoke its qualified native converter and retain conversion reports and dependency evidence.
- **Source evidence:** obtain the relevant source text/tables and traceable citations, then supply candidate TIDAS seed rows alongside that evidence. Use available document or research skills where needed; record the resolved source and artifact provenance. The seed must be one of the selected sources. Retrieved text or candidate rows alone do not prove readiness to write.

## Continue from the current result

Read `status`, `blockers`, `permissions`, `artifacts` and ordered `next_actions` together. A command action supplies the exact executable, argv, working directory and binding. Preserve those fields; use argv-based execution without a shell. A human action describes the particular missing input or decision. Do not reconstruct internal commands from old reports or scan a task directory for something executable.

For each current human action, first check whether supplied evidence, an applicable registered decision, or independent investigation can resolve it. Ask the person when their evidence or judgment is actually needed. Explain in everyday language what is missing, what it affects, the recommended next step and one precise question. Offer a few distinct choices when useful, while accepting a natural-language answer, additional evidence or “I don't know; investigate first.” State units, period and object scope for requested numbers. Keep the default message brief, but include every limitation, uncertainty and source distinction that could change the choice; give the current evidence or technical diagnostics as followable detail. Group tightly related questions and prioritize the current blocker. An unanswered recommendation is never consent, and “investigate first” is a next action, not proof that the missing fact is resolved.

Use only the selected runtime's registered interaction actions and artifacts to retain the person's original words, the interpreted decision, provenance and applicability. Make the interpretation visible in normal language as work continues; ask a focused follow-up only when an ambiguity materially changes the result. Keep AI assumptions, source facts and user instructions distinct. A decision must reach the affected authoring context and its adoption evidence through Foundry; a chat-only answer does not resolve a registered blocker. If the selected runtime lacks the required interaction contract, report the capability gap instead of guessing an input flag, editing task state or claiming the answer will survive resume. Independent work can continue through current returned actions, but partial assessment coverage cannot be represented as completion.

When a concrete semantic work item is current, load `$foundry-tidas-authoring` if available and pass its complete package and context. If that internal skill is unavailable, use the work item's supplied instructions and template; do not invent a dependency on a sibling skill directory. Preserve identity, classification and location decisions as their own decision types. Retain source-language information and evidence-backed English for required fields. Unresolved mandatory scientific information remains a blocker.

If a user changes a goal, principle or evidence, record the new instruction with its scope through the current public interaction action. Explain which results may change. Reconsider the affected work and downstream checks using the runtime's registered dependency evidence; broaden review when the affected scope cannot be proved. Do not silently elevate a task-local choice into a global rule or replay an already consumed write. Current attempts, before-images and readback requirements still govern any later revision.

Unknown values stay unknown. `annualSupplyOrProductionVolume` is written only from explicit annual source evidence — never derived from a reference `meanAmount`/`resultingAmount`, a default unit, a per-year assumption or a `9999` sentinel — and a genuinely unknown volume is normalized to the supported empty array with a row-level evidence gap. Read the runtime's validation-layer report (schema, authoring evidence, content, multilingual) and gap entries instead of re-implementing validation, and treat a structurally valid unknown value as neither authoring evidence nor provider-weight eligible. Reuse the existing draft's stable identity: resuming a task never increments a version or mints a new UUID. This entry's qualified Foundry 0.1.10 runtime provides the bounded existing-owner-draft repair lane (Foundry #171, released by #185 as `1e4f48bf8359f5e9bacba5741d15b7ff78f2eb41`), consuming the bounded existing-Process metadata admission from CLI #283 that is published since CLI 0.1.19: use it only through the bounded lane and the `next_actions` the runtime returns, with its fresh before-image and exact owner/state authorization, one consumed attempt, no replay, unknown outcomes read back, publication left false and no scientific field changed — never write the repair contract or its report yourself. Keep dataset prose free of workflow, approval or revision chatter. Installed support is separate from merged source behavior: the wrapper's pinned published CLI `0.1.20` and this entry's adjacent lock for independently qualified Foundry `0.1.10` (its bundled CLI is the Foundry owner's own `0.1.19`, not the wrapper pin) carry the unknown-`[]` policy, the validation-layer report and the repair entry in their released sources, but the runtime you actually invoke is the authority: an installed runtime that still emits a numeric sentinel such as `9999`, or whose report lacks those fields, is a stop condition: report qualified adoption as incomplete. Never hand-fabricate, overwrite or delete the empty array in the dataset. Never edit or discard the runtime report. Never bypass its gate.

Submit decision or patch files through the current `--semantic-input` contract described in the reference. Foundry validates their evidence, context and hashes, applies them deterministically, and reassesses the resulting rows. Do not edit registered source/final rows, reports or task indexes to make a gate pass.

On a qualified interaction-aware runtime, bind the semantic descriptor to the current indexed interaction-state SHA-256 and include exactly the applicable decision IDs for each submitted work item. Use the IDs returned with the authoring files; do not infer adoption from a chat answer or add unsupported fields to the decision/patch file. Confirm the accepted semantic result records the adopted decisions. Keep the distributed Foundry 0.1.10 lock's older semantic schema separate from this newer contract.

## Select reference evidence

When the current task needs explicit Process QA references or a reviewed exact-reference choice, use the public `--reference-input` descriptor in the workflow reference. Select the actual task/actor/current row manifest and every file digest; keep reference reviews separate from write approval. Use the qualified runtime's supplied schema and owner-produced consumer payload facts. Do not infer an exception, discover sibling files or replace a missing reference with a guessed version.

Foundry retains selected snapshots and carries their binding through QA, verification and readback. A new selection invalidates the prior finalization/approval projection; let the runtime revalidate an existing grant within its unchanged scope. Prepared or consumed scopes cannot replace their evidence. Missing or changed proof stays blocking. If the selected runtime does not support the descriptor, obtain the qualified successor distribution rather than using an internal command or editing its lock.

## Authorization, recovery and completion

Runtime installation, login and semantic acceptance do not authorize a database write. Reuse an existing valid approval within its exact scope. When the runtime reports missing authority, present the concrete current task, destination, dataset scope and requested actions for approval; select only the corresponding current authorization artifact. Never fabricate a grant or broaden one through a new request or revision.

For an explicitly selected native insert-only Flow, Process or Source scope, the final-row authorization descriptor may also bind the CLI execution-contract file and its raw SHA-256. Use the exact current final rows, project/account and draft-state intent; the contract does not grant permission. Follow the runtime's sealed action and original-attempt recovery instead of running the writer directly or constructing a replacement contract after an uncertain write.

If authentication is required, follow the trusted CLI/host's browser-login action and verify the intended account through its redacted status/doctor evidence. Do not read session contents, collect credentials or change the registered destination implicitly.

After an interrupted or uncertain write, continue the original task's returned readback/recovery action. Do not retry the mutation directly, clear attempt records or create a new workspace to reset it. Keep completed scopes and their original evidence while dependent scopes continue.

Report completion only when the current public task reports `completed` and its retained completion/readback evidence covers the requested scope. Local validation, an empty work queue, a child exit code or a successful upload alone is insufficient. Otherwise report the exact remaining blocker and next action, with the task ID and evidence paths needed to continue.

With the result, give a short decision recap derived from the current registered task evidence: the goal and scope, what was completed, the user's key requirements and choices, the AI assumptions, how they affected the outcome, and open questions or limits. Include partial completion plainly. Use ordinary language and keep technical evidence available for inspection; do not add a second completion-confirmation gate. A later correction follows the task's current interaction and revision actions for the affected work.
