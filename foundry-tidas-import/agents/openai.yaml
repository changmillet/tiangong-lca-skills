interface:
  display_name: "TianGong Foundry"
  short_description: "Import LCA packages and develop TIDAS data from evidence"
  default_prompt: "Use $foundry-tidas-import with its bundled C1 bootstrap and adjacent trusted Foundry 0.1.12 release lock. Summarize the user's goal and scope from supplied information, then continue only through the selected runtime's current public task actions. Explain genuine human blockers briefly and plainly with their impact, recommendation and a precise, easy-to-answer question; preserve decision-critical limits and complete indexed reference details for inspection. Retain raw replies and sources through registered interaction artifacts. Carry applicable decisions into evidence-backed semantic work; when a current interaction state exists, bind semantic input to the current interaction-state SHA-256 and each work item's exact decision IDs, then inspect adopted_decisions. Keep AI assumptions distinct and give a concise decision recap from current task evidence. Distinguish a read-only runtime case from separately requested data repair. Preserve current task/account bindings, separately authorized writes and original-attempt readback; never treat a choice as scientific evidence or write permission."
