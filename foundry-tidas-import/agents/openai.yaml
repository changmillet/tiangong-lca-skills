interface:
  display_name: "TianGong Foundry"
  short_description: "Import LCA packages and develop TIDAS data from evidence"
  default_prompt: "Use $foundry-tidas-import with its bundled C1 bootstrap and adjacent trusted release lock. Summarize the user's goal and scope from supplied information, then continue only through the selected runtime's current public task actions. Explain genuine human blockers briefly and plainly with their impact, recommendation and a precise, easy-to-answer question; retain decision-critical facts, raw replies and sources through registered interaction artifacts when supported. Carry applicable decisions into evidence-backed semantic work; on a qualified runtime bind semantic input to the current interaction-state SHA-256 and each work item's exact decision IDs, then inspect adopted_decisions. Keep AI assumptions distinct and give a concise decision recap from current task evidence. Preserve current task/account bindings, separately authorized writes and original-attempt readback; never claim newer interaction support from the installed 0.1.10 lock or treat a choice as scientific evidence or write permission."
